<?php
// Exemplo para DESENVOLVIMENTO LOCAL (Docker Compose)

// ===== TIMEZONE E ENCODING =====
date_default_timezone_set("America/Sao_Paulo");
ini_set("default_charset", "UTF-8");
ini_set("post_max_size", "4096M");
ini_set("upload_max_filesize", "4096M");

// ===== BANCO DE DADOS =====
define("DB_HOST", "mysql");
define("DB_NAME", "mysql_driftex");
define("DB_USER", "user_driftex");
define("DB_PASS", "<DB_PASS_LOCAL>");

// ===== REDIS =====
define("REDIS_HOST", "redis");
define("REDIS_PORT", 6379);
define("REDIS_PREFIX", "driftex:site:");
define("REDIS_DATABASE", 0);
define("REDIS_ENABLED", true);
define("REDIS_DEFAULT_TTL", 3600);

// ===== KAFKA =====
define("KAFKA_HOST", "kafka");
define("KAFKA_PORT", "9092");
define("KAFKA_TOPIC_EMAIL", "driftex_site_emails");
define("KAFKA_CONSUMER_GROUP", "driftex-email-worker-group");

// ===== EMAIL (SMTP) =====
define("mail_from_name", "Driftex Local");
define("mail_from_mail", "noreply@driftex.local");
define("mail_from_host", "<MAIL_HOST_LOCAL>");
define("mail_from_port", "<MAIL_PORT_LOCAL>");
define("mail_from_user", "<MAIL_USER_LOCAL>");
define("mail_from_pwd", "<MAIL_PASS_LOCAL>");

// ===== APLICAÇÃO =====
define("cAppKey", "driftex_site_session");
define("cTitle", "Driftex");
define("cAppRoot", "/");
define("cRootServer", sprintf("%s%s", $_SERVER["DOCUMENT_ROOT"], constant("cAppRoot")));
define("cRootServer_APP", sprintf("%s%s", $_SERVER["DOCUMENT_ROOT"], constant("cAppRoot") . "../app"));
$_detected_protocol = (!empty($_SERVER['HTTP_X_FORWARDED_PROTO']) && strtolower($_SERVER['HTTP_X_FORWARDED_PROTO']) === 'https') || (!empty($_SERVER['HTTPS']) && strtolower((string)$_SERVER['HTTPS']) !== 'off') ? 'https' : 'http';
define("cFrontend", sprintf("%s://%s%s", $_detected_protocol, $_SERVER["HTTP_HOST"] ?? "driftex.local", constant("cAppRoot")));
define("cAssets", sprintf("%s%s", constant("cFrontend"), "assets/"));

// ===== UPLOAD =====
define("UPLOAD_DIR", "/var/www/driftex/site/public_html/assets/upload/");
define("UPLOAD_MAX_SIZE", 10);
define("UPLOAD_ALLOWED_TYPES", "jpg,jpeg,png,gif,pdf,doc,docx,xls,xlsx");

// ===== SESSÃO =====
define("SESSION_LIFETIME", 7200);
define("SESSION_USE_REDIS", false);

// ===== KEYS =====
define("binanceAPIKey", "<BINANCE_API_KEY_LOCAL>");
define("binanceSecretKey", "<BINANCE_API_SECRET_LOCAL>");

// ===== INICIALIZAR REDIS =====
if (REDIS_ENABLED && class_exists("RedisCache")) {
    try {
        $GLOBALS["redis"] = RedisCache::getInstance();
    } catch (Exception $e) {
        error_log("Redis Error: " . $e->getMessage());
        $GLOBALS["redis"] = null;
    }
}
